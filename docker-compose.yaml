services:
  userdata-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${USERDATA_DB_USER}
      POSTGRES_PASSWORD: ${USERDATA_DB_PASSWORD}
      POSTGRES_DB: ${USERDATA_DB_NAME}
    # ports:
    #   - "5433:5432"
    expose:
      - "5432"
    volumes:
      - userdata_db_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${USERDATA_DB_USER} -d ${USERDATA_DB_NAME}" ]
      interval: 5s
      timeout: 5s
      retries: 5

  spam-ham-api:
    image: ghcr.io/rifqiansharir/indobert-spam-ham-api:v1
    expose:
      - "8000"
    healthcheck:
      test: [ "CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')" ]
      interval: 5s
      timeout: 1m
      retries: 5
      start_period: 30s

  public-issue-api:
    image: ghcr.io/rifqiansharir/indobert-public-issue-api:v1
    expose:
      - "8000"
    healthcheck:
      test: [ "CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')" ]
      interval: 5s
      timeout: 1m
      retries: 5
      start_period: 30s

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: >
      sh -c "
      uv run alembic upgrade head && 
      uv run uvicorn app.main:app --host 0.0.0.0 --port 8000
      "
    environment:
      SPAM_HAM_API_URL: http://spam-ham-api:8000/predict
      PUBLIC_ISSUE_API_URL: http://public-issue-api:8000/predict
      SESSION_SECRET_KEY: ${SESSION_SECRET_KEY}
      USERDATA_DB_USER: ${USERDATA_DB_USER}
      USERDATA_DB_PASSWORD: ${USERDATA_DB_PASSWORD}
      USERDATA_DB_NAME: ${USERDATA_DB_NAME}
      USERDATA_DB_HOST: userdata-db
      USERDATA_DB_PORT: 5432
    expose:
      - "8000"
    depends_on:
      userdata-db:
        condition: service_healthy
      spam-ham-api:
        condition: service_healthy
      public-issue-api:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_started

volumes:
  userdata_db_data:
